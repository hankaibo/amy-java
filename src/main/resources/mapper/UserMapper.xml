<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mypandora.springboot.modular.system.mapper.UserMapper">
    <resultMap id="UserResultMap" type="cn.mypandora.springboot.modular.system.model.po.User">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="nickname" jdbcType="VARCHAR" property="nickname"/>
        <result column="real_name" jdbcType="VARCHAR" property="realName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="salt" jdbcType="VARCHAR" property="salt"/>
        <result column="state" jdbcType="INTEGER" property="state"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="sex" jdbcType="TINYINT" property="sex"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="modify_time" jdbcType="TIMESTAMP" property="modifyTime"/>
    </resultMap>
    <sql id="base_column_list">
        id, username, nick_name, password, salt, state, avatar, email, phone, sex, last_login_time, create_time, modify_time
    </sql>

    <select id="selectRoleByUserId" parameterType="java.lang.Long" resultMap="string">
        SELECT r.code
        FROM sys_user u
                 JOIN sys_user_role ur ON u.id = ur.user_id
                 JOIN sys_role r ON r.id = ur.role_id
        WHERE u.id = #{userId,jdbcType=BIGINT}
    </select>

    <select id="selectUserByRoleId" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT u.*
        FROM sys_user u
    </select>

</mapper>